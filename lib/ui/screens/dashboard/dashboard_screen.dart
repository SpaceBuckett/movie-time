import 'package:flutter/material.dart';

class DashboardScren extends StatelessWidget {
  const DashboardScren({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard Section'),
      ),
    );
  }
}
